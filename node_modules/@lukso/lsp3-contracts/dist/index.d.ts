import { Verification } from '@lukso/lsp2-contracts';

type LSP3ProfileMetadataJSON = {
    LSP3Profile: LSP3ProfileMetadata;
};
type LSP3ProfileMetadata = {
    name: string;
    description: string;
    links?: LinkMetadata[];
    tags?: string[];
    profileImage?: ImageMetadata[];
    backgroundImage?: ImageMetadata[];
    avatar?: AssetMetadata[];
};
type ImageMetadata = {
    width: number;
    height: number;
    verification?: Verification;
    url: string;
};
type LinkMetadata = {
    title: string;
    url: string;
};
type AssetMetadata = AssetFile | DigitalAsset;
type AssetFile = {
    url: string;
    verification?: Verification;
    fileType?: string;
};
type DigitalAsset = {
    address: string;
    tokenId?: string;
};
declare const LSP3SupportedStandard: {
    key: string;
    value: string;
};
declare const LSP3DataKeys: {
    SupportedStandards_LSP3: string;
    LSP3Profile: string;
};

export { type AssetFile, type AssetMetadata, type DigitalAsset, type ImageMetadata, LSP3DataKeys, type LSP3ProfileMetadata, type LSP3ProfileMetadataJSON, LSP3SupportedStandard, type LinkMetadata };
